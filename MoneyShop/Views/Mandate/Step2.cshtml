@{
    ViewData["Title"] = "Informare Costuri";
    Layout = "_Layout";
}

<div class="container my-5">
    <div class="row justify-content-center">
        <div class="col-lg-8">
            <div class="card">
                <div class="card-header">
                    <h2 class="mb-0">💳 Costuri interogare și analiză</h2>
                </div>
                <div class="card-body">
                    <p class="lead">
                        Interogările ANAF și Biroul de Credit, precum și analiza MoneyShop, implică costuri operaționale care nu sunt gratuite.
                    </p>

                    <div class="alert alert-warning">
                        <h5>Costuri estimate:</h5>
                        <ul class="mb-0">
                            <li>Interogare ANAF: <strong>15 RON</strong></li>
                            <li>Interogare Biroul de Credit: <strong>25 RON</strong></li>
                            <li>Analiză MoneyShop: <strong>10 RON</strong></li>
                            <li class="mt-2"><strong>Total: 50 RON</strong></li>
                        </ul>
                    </div>

                    <p>Aceste costuri sunt suportate de către tine și sunt afișate transparent înainte de confirmare.</p>

                    <form method="post" asp-action="Step2">
                        <div class="consent-checkbox required">
                            <label>
                                <input type="checkbox" name="acceptCosts" id="acceptCosts" required>
                                <span>Confirm că am fost informat despre costurile interogărilor și ale analizei MoneyShop și sunt de acord cu acestea.</span>
                            </label>
                        </div>

                        <div class="mt-4">
                            <a href="@Url.Action("Step1", "Mandate")" class="btn btn-outline-secondary">Înapoi</a>
                            <button type="submit" class="btn btn-primary float-end">Continuă</button>
                        </div>
                    </form>
                </div>
            </div>
        </div>
    </div>
</div>

@section Scripts {
    <script>
        document.getElementById('acceptCosts').addEventListener('change', function() {
            const submitBtn = document.querySelector('button[type="submit"]');
            submitBtn.disabled = !this.checked;
        });
        
        document.querySelector('button[type="submit"]').disabled = true;
    </script>
}
